
  # Rule Engine

  This is a code bundle for Rule Engine. The original project is available at https://www.figma.com/design/yseuMijNa8LXMf6OWLGMWp/Rule-Engine.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  