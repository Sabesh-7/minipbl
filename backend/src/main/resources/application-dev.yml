spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/rule_engine}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:Sabesh123}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: INFO
    com.sabes.workflow.rule_engine: DEBUG

app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:3000,http://localhost:4173}
